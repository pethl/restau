<%= bootstrap_form_for(@accruel_rate) do |f| %>
  <% if @accruel_rate.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@accruel_rate.errors.count, "error") %> prohibited this accruel_rate from being saved:</h2>

      <ul>
      <% @accruel_rate.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.date_select :effective_date %>
    <%= f.text_field :rate %>

  <div class="actions">
    <%= f.submit class: "btn btn-primary" %>
  </div>
<% end %>
