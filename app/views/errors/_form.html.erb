<%= bootstrap_form_for(@error, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-5") do |f| %>
  <% if @error.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@error.errors.count, "error") %> prohibited this error from being saved:</h2>

      <ul>
      <% @error.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <%= f.text_field :ref %>
    <%= f.text_area :msg %>
    <%= f.text_area :desc %>
    
    <div class="form-group">
      <div class="col-sm-offset-2 col-sm-10">
  <div class="actions">
  <%= f.submit class: "btn btn-primary" %>
  </div>
</div>
  </div>
<% end %>
